# Uncomment the next line to define a global platform for your project
platform :ios, '10.2'

target 'FitCal Watch' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FitCal Watch

end

target 'FitCal Watch Extension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FitCal Watch Extension

end

target 'FitCal' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FitCal
	pod 'CVCalendar'
	pod 'DropdownMenu'

  target 'FitCalTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
